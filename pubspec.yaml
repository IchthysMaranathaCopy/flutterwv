name: webview_deneme
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2
  url_launcher: ^6.2.5
  flutter_native_splash: ^2.4.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/splash.jpg

flutter_native_splash:
  color: "#0277BD"  # Splash ekranı arka plan rengi
  image: assets/splash.jpg
  android: true
  ios: true
  android_12:
    image: assets/splash.jpg
    icon_background_color: "#0277BD"
